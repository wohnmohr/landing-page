---
export interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<div
  class={`mx-auto w-full min-w-0 max-w-6xl px-4 sm:px-6 md:px-8 lg:px-6 ${className}`}
>
  <slot />
</div>
